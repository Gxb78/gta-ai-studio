# Media worker — Phase 1 embarquée

Le hashing, FFprobe, l’intégrité et le proxy FFmpeg sont actuellement exécutés par le worker embarqué du sidecar dans `services/api/src/gta_studio_api`. L’extraction audio/frames et OpenCV appartiennent aux phases suivantes.
